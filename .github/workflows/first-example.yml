name: My First Workflow

on: Push

jobs:
    first_job:
        runs-on: ubuntu-latest
        setps:
        - name: Welcome message
          run: echo "My first Github Actions Job"

        - name: List files
          run: ls

        - name: Read file
          run: cat README.md